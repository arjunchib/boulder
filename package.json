{
  "name": "boulder",
  "version": "0.1.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -r esbuild-runner/register playground/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.23",
    "esbuild-runner": "^2.2.1"
  }
}
